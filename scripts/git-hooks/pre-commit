#!/bin/bash
# 快速通道：lint + 单元测试（≤ 10s）

echo "Running pre-commit checks..."

# 后端
cd backend && uv run ruff check app/
cd backend && uv run pytest ../tests/unit/backend/ -v -q --tb=no

# 前端
cd frontend && pnpm lint
cd frontend && pnpm test:unit --run

echo "Pre-commit checks passed!"